import React from "react";
import { Platform } from "react-native";
import Icon from "react-native-vector-icons/Ionicons";

const Logo = () => (
  <Icon name="logo-github" color="#ccc" size={35} style={{}} />
);

export default Logo;
