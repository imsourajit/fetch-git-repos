import React from "react";
import { Platform } from "react-native";
import Icon from "react-native-vector-icons/Ionicons";

const IconSet = props => (
  <Icon name="md-menu" color="#ccc" size={35} style={{ margin: 10 }} />
);

export default IconSet;
